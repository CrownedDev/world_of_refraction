# Deferred-Set Triage — 2026-05-29

Companion to `docs/Gaps/IntegrationGaps.md`. The catalog is the source of truth for *what* each gap is; this doc is a planning lens over the **22 not-fully-resolved** gaps — *can it be picked up now, how big is it, does it touch the pitch*. Categories verified against current `main` (not just the catalog text). **Do not treat this as the catalog** — statuses here are session-planning judgements, dated and disposable.

Starting list is the 2026-05-28 count survey: 20 fully-unresolved + 2 partial-with-residue (**4.1**, **10.6**).

> **Updated 2026-05-29** (two passes). After the gap discussion, the original 7 DOABLE-NOW resolved to: 8.1 → CLOSED (working as designed); 3.3 → BLOCKED-EXTERNAL; 10.7 + 3.1(camera half) + 7.3 → BLOCKED-DESIGN. A later build-prep survey then rescoped **10.6 residue DOABLE-NOW → BLOCKED-DESIGN** (Crown's real invalidity — element-source + weapon-type/ability mismatch — is undetected today; gated on a scope rung + the Innate-fallback intent). **Net: 7 DOABLE-NOW → 1 (`7.1`).** See catalog commits `docs(gaps): reclassify 5 gaps per session discussion` and `docs(gaps): reclassify 10.6 residue — real scope discovered during build prep`.

## Summary

- **Total unresolved:** 21 *(was 22; 8.1 closed as working-as-designed)*
- **DOABLE-NOW:** 1 — `7.1`
- **BLOCKED-EXTERNAL:** 8 — `3.3`, `4.1`(residue), `5.1`, `5.2`, `6.2`, `6.3`, `9.2`, `10.3`
  - Waiting on: **revive VFX/SFX/UI/combat-log** (3.3) · **combat-log UI** (5.2, part of 5.1) · **loadout UI** (5.1) · **VFX/SFX assets** (4.1 transform stinger, 6.2 interrupt feedback, 6.3 teleport FX) · **BD Darkness spell content authoring** (9.2) · **spell-scroll item feature** (10.3, unreachable today)
- **BLOCKED-DESIGN:** 9 — `2.1`, `2.2`, `2.3`, `3.1`(camera half), `7.3`, `8.2`, `9.1`, `10.6`(residue), `10.7`
- **BLOCKED-VERIFICATION:** 3 — `1.1`, `1.2`, `3.2`
- **CLOSED:** 1 — `8.1` (working as designed — BP-via-`OnCombatStartedUI` extension point)

> Bucket totals: 1 + 8 + 9 + 3 = 21 unresolved, + 1 closed = 22 originally surveyed. 3.1 is counted once (in BLOCKED-DESIGN for its camera half); its asset half is also tracked under BLOCKED-EXTERNAL's waiting-on list but not double-counted.

> Note on the buckets: several "blocked" gaps have a *trivial doable sub-step* (e.g. a debug-log consumer) but their **meaningful value** is gated — those are filed by the gate, not the sub-step, with the sub-step noted in the rationale.

---

## By bucket

### DOABLE-NOW (1 gap)

The one gap confirmed real and unblocked. *(10.6 was here in the morning pass; the build-prep survey rescoped it to BLOCKED-DESIGN — see below.)*

| Gap | Description | Scope | Pitch | Rationale |
|-----|-------------|-------|-------|-----------|
| **7.1** | SkillEffectManager passive-layer effect handlers are switch-stubs | MEDIUM | YES (if affected effects demoed) | Code-only — stub block confirmed at `SkillEffectManager.cpp:1112`. **High leverage:** 10.5's StatusMultiplierBuff/Debuff aggregation is already wired but queries `0.0f` until these handlers populate real values (per 10.5 cross-link). Implementing them activates an already-landed fix for free. |

### BLOCKED-EXTERNAL (8 gaps)

Grouped by what they're waiting on.

**Waiting on revive VFX/SFX/UI/combat-log:**
- **3.3** — `UCharacterDataComponent::OnResurrected` no subscribers. **Reclassified from DOABLE-NOW (2026-05-29).** The revive mechanic exists (S-tier healing item + effects) and the producer fires correctly; what's missing is *feedback*, and every plausible consumer (resurrection VFX, SFX, UI flash, combat-log entry) is itself blocked on assets/systems that don't exist. C++ side is complete. A debug-log consumer is the only doable sub-step and carries no real value — so this is gated by the missing feedback surfaces, same bucket as 5.1/5.2.

**Waiting on combat-log UI:**
- **5.2** — `ItemExecutor::OnItemUsed` / `OnGambleResult` no subscribers. Confirmed zero `AddDynamic`. The intended consumers are a combat-log entry ("used Garnet S on X") and an Amethyst gamble-result surface — neither exists.

**Waiting on loadout UI:**
- **5.1** — `LoadoutComponent::OnLoadoutChanged` / `OnLoadoutItemUsed` / `OnValidationFailed` no subscribers. Confirmed zero `AddDynamic`. Meaningful consumers are loadout/inventory UI. *(Sub-step: `OnValidationFailed` could get a debug-log consumer now, but real value needs the UI.)*

**Waiting on VFX/SFX assets:**
- **4.1** (residue) — `OnTransformed` stinger (UI/SFX) + `OnOverloadDamage` per-tick feedback. The display surfaces from sweep-5 landed; what remains is the runtime-transform narrative beat (stinger) and per-tick overload feedback — both need assets + a UI/SFX design. **Pitch: YES if a runtime BD transform happens on stage.**
- **6.2** — `OnMovementCancelled` no subscribers. Needs an "attack interrupted" feedback surface.
- **6.3** — Approach/return teleport VFX TODOs (`CombatMovementComponent.cpp:159/170`). Pure asset gap.

**Waiting on content authoring:**
- **9.2** — BD `InnateSpells` (Darkness pool) empty in `DA_Inventory_BD.uasset` (LFS, not readable here). Designer must author ≥1 Darkness spell or the Refractions submenu shows nothing. **Pitch: YES if BD is demoed.**

**Waiting on an unbuilt feature:**
- **10.3** — `ESpellSource::Item` stub case. Unreachable today (zero producers). Only matters once a spell-scroll item feature is designed/built. No action now.

### BLOCKED-DESIGN (9 gaps)

| Gap | Open design question |
|-----|----------------------|
| **2.1** | DefensePromptWidget — **deferred pending Crown** (just filed): per-context defense options? timing-indicator style? input scheme (couples to 1.1's new PC)? no-input fallback? visual style / HUD relationship? **Pitch-blocker** for any demo with a player defending. |
| **2.2** | `OnDefenseWindowOpened` no subscriber — **collapses to 2.1** (its consumer *is* DefensePromptWidget). Resolves the moment 2.1's design lands. **Pitch.** |
| **2.3** | `OnDefenseInputReceived` / `OnParryReflect` / `OnDefenseCueTriggered` — which of these game-feel surfaces do designers actually want, and with what SFX/VFX? (`OnDefenseCueTriggered` is also never broadcast — needs a producer too.) |
| **3.1** (camera half) | **Reclassified from DOABLE-NOW (2026-05-29).** Wiring `OnActionStarted`/`OnActionCompleted` to the camera is a *small* implementation; the work is the design question — **what should the camera DO when an action fires?** Deferred pending camera-feel direction. (Its other half — kill-feed/healing-VFX — is BLOCKED-EXTERNAL, tracked separately.) |
| **7.3** | **Reclassified from DOABLE-NOW (2026-05-29).** Action-camera transition (`CombatCameraManager.cpp:466`). Impl is small; camera language/feel is the unresolved work. Moves in lockstep with 3.1's camera half — decide camera direction, then do both. |
| **8.2** | `CharacterPanelWidget::OnPanelHovered` / `OnPanelClicked` no C++ subscribers — is panel-hover target-picking meant to be a C++ flow, or is BP-mediated targeting the intended design? |
| **9.1** | Cycle-source UI is Breakthrough-only — should Refractions / ResonateWeapon / ResonateRing ever support source cycling? Catalog flags this as "verify intent — may not be a gap." |
| **10.6** (residue) | **Rescoped from DOABLE-NOW (2026-05-29 build-prep survey).** The "clear what the validator flags" plan assumed ownership was the only invalidity — but Crown's real concern is **element-source mismatch** and **weapon-type/ability mismatch**, which the validator does NOT detect. Two unanswered questions gate it: **(1) scope rung** — A (ownership-only, SMALL) / B (+ weapon-type, wire orphaned `FWeaponLoadoutEntry::ValidateAbilities`, SMALL-MED) / C (+ element-source, new detection, MEDIUM); **(2)** is `ResolveSpellSource`'s silent `Innate` fallback graceful degradation (allowed) or invalid (rejected)? — determines whether element-source mismatch is even a failure. |
| **10.7** | **Reclassified from DOABLE-NOW (2026-05-29).** `AutoPopulateLoadout` is dumb — but "smart" is undefined until the **character-to-enemy loadout source** is decided: when a player uploads a character without explicit loadouts, where does the loadout come from? Direction A (max power), B (thematic), C (constrained random), or hybrid? **Not** the loadout-as-difficulty model (that's authored-enemies only — see `docs/Design/LoadoutDifficultyModel.md`). |

### BLOCKED-VERIFICATION (3 gaps)

| Gap | Verification that would resolve it |
|-----|------------------------------------|
| **1.1** | **Which PC class does the combat game mode actually use in PIE?** If `ACombatPlayerController` is live → gut/gate the test path; if not → it's dead weight. The gut-vs-gate call can't be made without that check. *(A defensive `bIsTestMode` fence is doable now regardless, but the real fix needs the PIE answer.)* **Pitch.** Couples to 2.1's input scheme. |
| **1.2** | **Does the BP-side player defense input actually work today, and which PC owns combat input?** Catalog says "works, presumed, unverified." Confirm in editor, then move the binding onto the real PC. Couples to 1.1. |
| **3.2** | **Does `BP_CombatOrchestrator` subscribe to `OnCombatResultReady` / `OnCombatStateChanged`?** Confirmed the only C++ binders are the test actor. If BP covers it → not a gap; if not → ship a C++ result-overlay. **Pitch: YES if no BP coverage.** |

### CLOSED (1 gap)

| Gap | Resolution |
|-----|------------|
| **8.1** | **CLOSED — working as designed (2026-05-29).** HUD spawn happens via `ACombatOrchestrator::OnCombatStartedUI`, a `BlueprintImplementableEvent` the orchestrator fires after `TurnManager` init and `BP_CombatOrchestrator` overrides to spawn the HUD. That's the **intentional BP extension-point pattern**, not a fragile workaround. The original "no production C++ spawns HUD widgets" framing was factually true but mischaracterized the design; `AHUDTestActor` is unrelated test-only scaffolding. No C++ port needed. Only residual: auditability (C++ can't grep-verify the BP wiring) — noted, not a task. |

---

## Recommended next-session menu

After the 2026-05-29 passes, only **one** gap is genuinely DOABLE-NOW. The morning's 5-item menu collapsed (8.1 closed, 3.1/7.3 deferred to camera-feel design, 3.3 asset-blocked), and the build-prep survey then rescoped 10.6 out too.

1. **7.1 — Implement SkillEffectManager passive-layer handlers** *(MEDIUM, highest leverage)*
   First move: read the stub block at `SkillEffectManager.cpp:1112`, enumerate which `ESkillEffectType` values are no-op, cross-ref against shipping crystals/spells to prioritize. Implementing these also activates 10.5's already-wired StatusMultiplier aggregation (currently dormant).

*(10.6 was the morning's #2 pick. Build-prep survey found Crown's real invalidity — element-source + weapon-type/ability mismatch — isn't detected today, so it's now BLOCKED-DESIGN pending a scope rung + the Innate-fallback intent. Not actionable until those land.)*

**Everything else is gated** — on design (camera direction for 3.1/7.3; character-to-enemy loadout source for 10.7; defense UX for 2.1/2.2/2.3), on verification (1.1/1.2/3.2 need PIE/BP checks), or on missing assets/systems (3.3, 4.1 residue, 5.1, 5.2, 6.2, 6.3, 9.2, 10.3). Picking any of those up now means doing the design/verification first, not the code.

---

## Changelog
- 2026-05-29 (initial) — Triage of the 22-gap deferred set; statuses ground-truth-checked against `main`. Branch: `main` (docs-only).
- 2026-05-29 (revised) — Post-discussion reclassification: 8.1 → CLOSED; 3.3 → BLOCKED-EXTERNAL; 10.7 + 3.1(camera) + 7.3 → BLOCKED-DESIGN; 10.6 confirmed DOABLE-NOW, scope MEDIUM → SMALL. DOABLE-NOW 7 → 2. See catalog commit `docs(gaps): reclassify 5 gaps per session discussion`.
- 2026-05-29 (build-prep) — 10.6 residue rescoped DOABLE-NOW → BLOCKED-DESIGN: build-prep survey found Crown's real invalidity (element-source + weapon-type/ability mismatch) is undetected today; gated on scope rung A/B/C + the `ResolveSpellSource` Innate-fallback intent. DOABLE-NOW 2 → 1 (just 7.1); BLOCKED-DESIGN 8 → 9. See catalog commit `docs(gaps): reclassify 10.6 residue — real scope discovered during build prep`.
